"use client";

import SectionWrapper from "./SectionWrapper";
import GalleryGrid from "./GalleryGrid";

export default function Gallery() {
  return (
    <SectionWrapper id="galerie" title="Galerie" subtitle="Terenuri, acțiune și atmosferă la Smash Padel">
      <GalleryGrid />
    </SectionWrapper>
  );
}
